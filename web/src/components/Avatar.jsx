


export default function Avatar({width= 8, image_url}) {
    
    return(
        <div className={`w-${width} h-${width} rounded-full bg-emerald-500 text-white flex items-center justify-center`}>B</div>
    )
}